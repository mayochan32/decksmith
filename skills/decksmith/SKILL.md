---
name: decksmith
description: 題材・構成・スタイルを個別に指定して、編集可能なPowerPointを生成する。参考画像や既存スライドの視覚表現を再現する場合、構成案をデザイン済みPPTXへ変換する場合、DeckSmithでプレゼンテーション作成を依頼された場合に使用する。PowerPointファイルを必要としない文章構成だけの依頼には使用しない。
---

# DeckSmith

利用者が指定する次の3要素を独立させたまま、完成度が高く編集可能な`.pptx`を作成する。

1. 題材：テーマ、対象者、目的、言語、根拠資料
2. 構成：スライド数、話の順序、各スライドの役割
3. スタイル：創造的な方向性、参考画像、全体設定とレイアウトカタログ

PPTXの生成と検証には内蔵のPresentations機能を使用する。利用者が画像生成を求めた場合、または選択したスタイルに画像が必要な場合は、内蔵の画像生成機能を使用する。APIキーやローカルGPUを要求しない。

## 対話方法

現在の依頼と添付ファイルから、題材・構成・スタイルを抽出する。欠けている情報のうち、完成物へ大きく影響する項目だけを質問する。3項目すべてが不明な場合は、題材、構成、スタイルの順に確認する。利用者は自然言語で指定でき、YAMLを直接編集する必要はない。

指定がない場合は、次の候補を提示する。

- 構成：提案、報告、研修、ストーリー、自由構成
- スタイル：ミニマル・エディトリアル、ダーク・シネマティック、温かみのある人物中心、参考画像ベース
- 画像：内容理解に明確な効果があるスライドだけで生成する。通常プランの利用上限を考慮し、既定では1資料あたり最大6枚とする

制作前に、解釈した内容を「題材」「構成」「スタイル」の3項目で簡潔に整理する。利用者がすでに資料作成を依頼しており、結果を左右する曖昧さがなければ、追加確認を待たずに進める。

## スタイルのコンパイル

参考画像を使う場合は、すべての画像を確認し、[スタイルシステム](references/style-system.md)に従って、参考記事形式の`style.yaml`を作る。

- `Overall Design Settings`にTone、Key Visual、Color Palette、Photography Style、Typography、Common Layout Rulesを記録する
- `Layout Variations (Catalog)`にType、Design、対応する`Applies To`を記録する
- DeckSmithが`style.yaml`を配色、文字サイズ、余白、装飾、レイアウトバリアントへ自動コンパイルする。内部仕様を利用者へ作らせない
- DeckSmithの既定値より、参考資料から読み取れる特徴を優先する
- 存命のアーティスト名を指定して模倣せず、観察可能な視覚的特徴として記述する

会社固有の素材は公開プラグインへ含めない。現在の許可済みワークスペース、または利用者が指定したローカルパスからのみ使用する。

## PowerPoint生成手順

1. `topic.yaml`、`structure.yaml`、`style.yaml`を作成する。[プロジェクトファイルの仕様](references/deck-spec.md)に従う。
2. 利用者が調査を求めた場合、または正確な内容に調査が必要な場合だけ情報を収集する。出典URLを対象スライドの引用情報へ記録する。
3. 生成画像ごとに、用途、主題、構図、スタイル、縦横比、文字用の余白を指定したプロンプトを作る。利用者が必要としない限り、画像内の文字やロゴを禁止する。
4. 内蔵の画像生成機能で画像を作り、プロジェクトの素材ディレクトリへ保存する。生成結果のパスを`structure.yaml`へ記録する。
5. 文字、表、グラフ、情報を伝える図解にはPowerPointのネイティブオブジェクトを使用する。正確なラベル、数値、出典を生成画像へ焼き込まない。
6. 次のコマンドで決定的なPPTX生成処理を実行する。

   ```bash
   "${RUNTIME_PYTHON:-${CODEX_PRIMARY_RUNTIME_PYTHON:-python3}}" scripts/create_deck.py \
     --topic <project>/topic.yaml \
     --structure <project>/structure.yaml \
     --style <project>/style.yaml \
     --output <project>/output/deck.pptx
   ```

   このSkillのディレクトリから実行する。Codexが提供するプレゼンテーション用ランタイムを使用する。スクリプトは現行の`CODEX_PRIMARY_RUNTIME_*`と従来の`RUNTIME_*`を自動認識する。組み込みPresentationsの場所も自動検出するため、利用者にパスを質問しない。ホストからPresentationsのSkillルートが得られており自動検出より確実な場合は、内部的に`DECKSMITH_PRESENTATIONS_SKILL_DIR`へ設定する。特殊な開発環境では`--presentations-skill-dir`も使用できるが、これは開発者向けの互換オプションである。最初の実行時に成果物操作が記録される。同じ操作内で修正版を生成する場合は`--skip-operation-marker`を追加し、記録処理を重複させない。

7. 生成された`<deck-name>.preview`ディレクトリのPNGを、すべて個別に原寸で確認する。[品質基準](references/quality-gates.md)に従い、問題があればYAMLまたはビルダーを修正する。修正版には新しい出力ファイル名を使用する。
8. 利用者がYAMLやプレビューも求めていない限り、最終版PPTXだけを返す。

## 重要な制約

- 外部の画像生成APIを呼び出さず、APIキーを要求しない。内蔵画像生成には、サインイン中の利用者プランとワークスペース設定が適用される。
- 機密資料を公開サイトや公開プラグイン素材へ無断で送信しない。
- 出典画像、会社ロゴ、製品画面、利用者が提供した写真は根拠資料として扱う。内容や縦横比を保持し、出典を発表者ノートへ記録する。
- 不明な事実を作らない。前提条件が結論に影響する場合は、スライド上で明示するか利用者へ確認する。
- プレゼンテーション用ランタイムまたは画像生成機能を使用できない場合は、公開サービスで代替せず、利用できない機能を具体的に説明する。
- 組み込みPresentationsの内部ディレクトリを作成・ダウンロード・指定するよう利用者へ求めない。検出できない場合は、Presentationsが利用可能なChatGPT／Codex環境での実行が必要だと説明する。
- 日本語資料では、PNGプレビューで日本語が表示されることを確認する。実行環境にCJKフォントがない場合は、編集可能な日本語テキストをPPTXに保持したうえで、視覚検査が未完了であることを伝える。プレビューが正常だと主張しない。
